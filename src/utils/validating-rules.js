export const passwordRule = /^(?=.*[A-Z].*)(?=.*[a-z].*)(?=.*[!@#$%^&*].*).{8,32}$/;
export const emailTooltipText =
  'The email must correspond to the following form: somename@example.com. This field is required.';
export const passwordTooltipText =
  'The password must contain from 8 to 32 characters including uppercase and lowercase Latin letters, number and symbols. This field is required.';
export const passwordConfirmationTooltipText =
  'The confirmed password must match the previous password. This field is required.';
